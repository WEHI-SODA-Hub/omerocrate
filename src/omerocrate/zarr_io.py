from pathlib import Path

import zarr


def create_zarr(outfile: Path) -> Path:
    """
    Create a new Zarr file with group for labels.
    """
    # TODO: Create a zarr file for labels -- infer data type and create zarr group UUID
    return Path()
